Сделай КОНСПЕКТ вопросов и ответов из переписки для FAQ.

## Правила извлечения:
1. Формулируй вопросы полно и понятно - чтобы было ясно о чём речь без контекста чата
2. Каждый элемент в `answers` должен быть самостоятельным полным ответом на вопрос, а не отдельным фактом или уточнением
3. Группируй все ответы одного `sender` на один вопрос вместе, даже если они в разных местах. В ответах пиши суть того что сказано, а не копируй фразы дословно.

## Критерий для ответов:
 - В `answers` пиши только то, что будет ПОЛЕЗНО человеку с таким же вопросом. Извлекай только то, что реально есть в переписке, запрещено добавлять что-то от себя.
 - СТРОГО ЗАПРЕЩЕНО: ответы, которые отсылают к другим источникам информации (чат, сайт, позвоните, напишите, уточните). Такие ответы считай бесполезными и полностью игнорируй.
 - Если ответов нет или все ответы бесполезные - оставь весь массив `answers` пустым: []
